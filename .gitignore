# Obsidian local UI state (not meaningful to sync)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Claudian AI 对话历史记录（本地隐私内容，不同步）
.claudian/sessions/

# 知识库网站（app/）的构建产物与依赖
app/node_modules/
app/dist-web/
app/release/

# .kb/ 里的个人数据：两个 SQLite 库（复习进度、真题作答、标注）和抓好的真题 JSON 一起进库，
# 换电脑 clone 下来就能用；WAL/SHM 是运行时文件、原始页面缓存能重抓，都不进
.kb/*.db-wal
.kb/*.db-shm
.kb/exams/raw/
schedule.json.migrated

# 设计稿的合成产物（约 2MB，由 app/design/*.dc.html 重新生成）
app/design/kaoyan-knowledge-base.html

# 真题抓取脚本的产物在 .kb/exams/（已被 .kb/ 覆盖）；截图辅助脚本不进库
app/.shot.cjs
.vscode/
